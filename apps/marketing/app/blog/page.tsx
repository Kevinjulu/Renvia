import Image from "next/image";
import Link from "next/link";
import { Footer } from "@/components/footer/Footer";
import { Navbar } from "@/components/nav/Navbar";
import { ARTICLES } from "@/lib/articles";

export default function BlogPage() { return <><Navbar/><main className="px-6 pb-24 pt-36"><div className="mx-auto max-w-content"><header className="max-w-3xl"><p className="text-xs uppercase tracking-[0.16em] text-faint">Renvia Journal</p><h1 className="mt-5 font-display text-5xl font-semibold tracking-[-0.045em] sm:text-7xl">Better visuals begin with better decisions.</h1><p className="mt-6 max-w-2xl text-lg leading-relaxed text-muted">Practical guidance for architects and designers using AI to communicate projects clearly.</p></header><div className="mt-16 grid gap-6 lg:grid-cols-3">{ARTICLES.map((article) => <Link href={`/blog/${article.slug}`} key={article.slug} className="group overflow-hidden rounded-2xl border border-hairline bg-white"><div className="relative aspect-[4/3] overflow-hidden bg-surface"><Image src={article.image} alt="" fill sizes="(max-width: 1024px) 100vw, 33vw" className="object-cover transition-transform duration-500 group-hover:scale-[1.03]"/></div><div className="p-6"><p className="text-xs text-faint">{article.category} · {article.readTime}</p><h2 className="mt-3 font-display text-2xl font-semibold leading-tight tracking-tight">{article.title}</h2><p className="mt-3 text-sm leading-relaxed text-muted">{article.dek}</p><span className="mt-6 inline-block text-sm font-medium">Read article →</span></div></Link>)}</div></div></main><Footer/></>; }
